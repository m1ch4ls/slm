{
  "name": "slm-reminder",
  "description": "Reminds Claude to use slm before and after Bash commands. Pre-Bash: suggests using slm for text processing (once per session via session_id tracking). Post-Bash: detects large outputs and suggests piping through slm.",
  "version": "1.0.1"
}
